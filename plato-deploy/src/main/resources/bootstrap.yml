aws:
  secretsmanager:
    enabled: true
    prefix: plato
    region: us-east-2
    name: plato
    fail-fast: false

spring:
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8100