import { HeadingPipePipe } from './heading-pipe.pipe';

describe('HeadingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeadingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
